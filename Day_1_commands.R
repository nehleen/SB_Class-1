# assigning values to variable: <-, =
# class, gives you the idea of the nature of the data
# names, assigning the elements of a vector, matrix into another variable/group
# cbind, c() or : means concatenate
# How to call a variable
# How to call a element from a vector, matrix etc.
# selecting sth element of a variable using []
# Some function: sum(), mean(), these are the base function which are builtin with R
# matrix creation, matrix <- , how to sort the values of a matrix by row or by column
# 